name: Build and Push Production Images

# Builds the backend and frontend Docker images and pushes them to the
# registry, tagged with the version from pyproject.toml and "latest",
# whenever changes are pushed to main.

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGISTRY_ADDRESS: containers.deltares.nl
  HARBOR_PROJECT: fair-data

jobs:
  build-and-push:
    name: Build and push ${{ matrix.service }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - service: backend
            context: fair-data-finder/backend
            file: fair-data-finder/backend/Dockerfile
            target: production
            image: backend-stars4water
          - service: frontend
            context: fair-data-finder/frontend
            file: fair-data-finder/frontend/dockerfile
            target: run-prod
            image: frontend-stars4water
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Extract metadata
        id: meta
        run: |
          VERSION=$(sed -n 's/^version = "\(.*\)"/\1/p' fair-data-finder/backend/pyproject.toml | head -n1)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_ADDRESS }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Build and push ${{ matrix.service }} image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          target: ${{ matrix.target }}
          push: true
          tags: |
            ${{ env.REGISTRY_ADDRESS }}/${{ env.HARBOR_PROJECT }}/${{ matrix.image }}:${{ steps.meta.outputs.version }}
            ${{ env.REGISTRY_ADDRESS }}/${{ env.HARBOR_PROJECT }}/${{ matrix.image }}:latest
